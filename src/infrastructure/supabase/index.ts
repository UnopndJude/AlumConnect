export { supabase, createServerSupabaseClient } from "./client"
export type { Database } from "./types"
export {
  createBrowserClient,
  createServerClient,
  getSession,
  getCurrentUser,
} from "./auth"
